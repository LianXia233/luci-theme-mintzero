#!/bin/sh
# mint ubus service (compatibility stub)
# Copyright (C) 2026 LianXia233
# Licensed to the public under the Apache License 2.0.
#
# NOTE (TZ-17, 2026-09-07): random wallpapers are fetched directly by the
# browser and custom images are local files - no server-side cache exists,
# so the old Bing prefetch was removed. The `refresh` method is kept as a
# stub so existing ACL grants and API clients keep working.

case "$1" in
	list)
		echo '{"refresh":{}}'
		;;
	call)
		case "$2" in
			refresh)
				echo '{"spawned":false,"mode":"random"}'
				;;
			*)
				echo '{"error":"invalid method"}'
				;;
		esac
		;;
esac
exit 0
